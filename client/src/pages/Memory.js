import React from 'react';

const Memory = ({socket}) => {
    return (
        <div>
            <h1>Memory Page</h1>
            <p>Welcome to the Memory Page!</p>
        </div>
    );
};

export default Memory;